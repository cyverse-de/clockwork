# Used by the test suite. Jobs are disabled so init-scheduler exercises only its
# initialization path without trying to register triggers against the in-memory
# Quartz scheduler.
clockwork.jobs.infosquito.indexing-enabled=false
clockwork.jobs.data-usage-api.indexing-enabled=false
