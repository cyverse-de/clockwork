[project]
name = "clockwork-functional-tests"
version = "0.1.0"
description = "Language-agnostic black-box functional/parity tests for clockwork"
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.0",
    "requests>=2.31",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
